spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/Diatonic_Academy
    driver-class-name: org.postgresql.Driver
    username: "JoseMiguel"
    password: "JMmf1996"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080
